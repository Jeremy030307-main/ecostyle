class User {
    constructor(id, fname, lname, email) {
      (this.id = id),
        (this.fname = fname),
        (this.lname = lname),
        (this.email = email)
    }
  }
  
export default User;